#include<iostream>

Construct(std::string key){
creation event construct after(std::string key):
              expr(string(...))  && result(key){}

ere:construct
@match{
    std::cout<<"construct a string!"<<std::endl;
    }
}

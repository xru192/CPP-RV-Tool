#include <stdio.h>
EXE(int* key){
	event exe_after after(int* key) :
              execution(% exe_after(...))&& result(key){}

    event exe_before before(int* key):
              execution(% exe_before(...)) && args(key){}


ere: exe_after exe_before

        @match {
          ;
        }
}

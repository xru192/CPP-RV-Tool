#include <stdio.h>
MF(void* key) {
	event malloc(void* key){}
	event free(void* key){}
	event endProg(char* loc){}
	ere:  (malloc   free)* malloc  endProg

	@match
	{
		printf("object not free at address %p\n",TARGET->key);

	}

}
